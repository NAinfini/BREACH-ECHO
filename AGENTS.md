# BREACH ECHO 贡献者与智能体工作准则

## 修改前必读

阅读 `README.md`、`docs/start-here.md`、`docs/governance/authoring-guide.md`、`docs/production/release-scope.md`、`docs/production/implementation-handoff.md`，以及当前任务对应的责任文档。文档与决策工作使用 `.agents/skills/breach-documentation/SKILL.md`。完整文档登记位于 `docs/governance/document-register.md`；不得用聊天摘要代替相关文件原文。

## 权威与受托判断

项目所有者是游戏制作新人，并明确委托普通技术与设计决策。为每个问题选出清晰、可辩护的方案；不要要求所有者选择网络库、序列化格式、目录结构、平衡系数或日常实现细节。在同一 Git 变更中记录决定、理由、淘汰方案、约束、影响与测试。

保留所有者的明确要求。`docs/governance/project-owner-decision-queue.md` 中的事项仍需所有者批准：最终叙事与角色身份、不可逆的品牌/美术/配音承诺、实际支出/合同/账户访问，以及修改明确要求。每个真正需要所有者回答的问题都必须附推荐方案。不得把受托决策描述为所有者直接确认。发现薄弱方案时要有依据地指出，而不是自动附和。

## 实现纪律

先完成一条薄而完整的可玩路径，再扩展内容。遵循里程碑依赖；文档不等于实现。不得为了假想的未来用户，预先搭建整套引擎、第二种模式、通用可视化编辑器或任意原生模组执行能力。有充分理由时允许重构架构。删除被替代的运行路径，不维护旧实现双轨。已经发布的玩家进度仍需明确的保留/迁移政策；“不保留旧代码”不等于可以破坏存档。

使用已选定的技术栈。核心玩法不得依赖 Steam ID、传输层回调、Unity 场景实例 ID、画面帧率、音频播放时长或特定模组市场。主机与客户端动作必须走同一条已验证命令路径。禁止隐藏式敌人缩放、丢弃结果的性能上限、无限资源漏洞、检查点反复读取漏洞和客户端自行签发奖励。

官方内容与受支持模组使用同一条已验证包路径。哈希不是信任签名。不受信内容不得访问任意 C#、原生 DLL、进程执行、文件系统、套接字、凭据、账户写入或协调服务签名密钥。没有明确再分发权时，绝不能把购买的授权资产上传到此公共仓库、公共 SDK、Workshop 示例或公共持续集成制品。

## 工作与验证

每项实质任务开始时，记录可持久化任务计划、规则 ID、验收测试和当前提交。在当前会话报告进展与阻碍。提交应小而完整；不得强制推送或覆盖其他贡献者的变更。除正常路径外，还要测试失败路径、回滚与重连。规则变化时同步更新责任文档、决策登记、待办与交接。

文档变更运行 `python3 tools/validate_docs.py`、其自测试，以及 `python3 tools/docs_audit.py`。游戏工具存在后，运行任务指定的 EditMode、PlayMode、构建、集成和设备测试。记录准确命令、提交/构建、种子、硬件、输入方式、设置和实测结果。不得虚构日志、截图、基准、外部审阅者或已通过的 Gate。不得仅为通过测试而用更弱阈值替换未通过的测试。

## 叙事审阅边界

项目所有者必须先审阅完整故事总览，再由一个真正全新、只读且不了解项目上下文的审阅者检查故事。不得把自己称为独立盲审者，也不得暗中批准四名角色候选。年龄、体型、外貌与声音尚未决定是所有者有意延后，并非可用刻板印象填补的空白。

## 任务结束时的交接

说明改动、提交、实际运行的测试、未运行测试及原因、已知风险和下一项可执行任务。将实现状态与设计权威分开记录。留下足够信息，使新人无需本次聊天也能继续。除非确实建立了已授权自动化，否则不得承诺无人值守的后续工作。
